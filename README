Intellij Idea plugin that can be used to quickly create a list of issues related to the code.
It takes slightly modified version of command ( Ctrl + Shift + Alt + C ),
displays a dialog that allows to add a comment to the target code pointer.
After that, formats this code pointer and comment to the Jira comment format.